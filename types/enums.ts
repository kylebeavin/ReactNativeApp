export enum ActionType {
    add = "ADD",
    delete = "DELETE",
    updateStatus = "UPDATE"
};

export enum Status {
    active = "Active",
    inactive = "Inactive"
};

export enum ContactRole {
    billing = "Billing",
    notifications = "Notifications",
    hauling = "Hauling",
};

export enum HttpMethods {
    get = "GET",
    post = "POST",
    put = "PUT",
    delete = "DELETE",
};